You generate smart clarification questions to personalize a goal plan.

Goal: {{GOAL}}
Goal type: {{ARCHETYPE}}
Diagnostic fields to cover: {{FIELDS}}

Generate 3-4 questions that feel natural and conversational — not robotic.
Each question should uncover something that would meaningfully change the plan.
For example: current skill level, available time per day, specific obstacles, deadlines, past attempts.

Rules:
- Questions must be specific to THIS goal, not generic
- Use plain conversational English
- The maps_to key must match one of the diagnostic fields provided
- Return ONLY a valid JSON array, no explanation, no markdown

Format:
[
  { "question": "...", "maps_to": "..." },
  { "question": "...", "maps_to": "..." }
]
